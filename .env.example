# PostgreSQL接続文字列（Supabase / Neonの接続情報を設定してください）
DATABASE_URL=postgres://user:password@host:5432/dbname?sslmode=require

# サーバーのポート番号（省略時は8080）
PORT=8080

# Anthropic APIキー（AI提案機能で使用。未設定でもサーバーは起動するがAI提案機能のみ利用不可になる）
ANTHROPIC_API_KEY=
